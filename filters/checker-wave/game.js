// Generated by LiveScript 1.2.0
(function(){
  'use strict';
  var assets, filters, filter, gPreload, gCreate, gUpdate, gRender, g;
  assets = '../../../phaser/examples/assets/';
  filters = '../../../phaser/filters/';
  gPreload = function(){
    g.load.image('s', assets + 'pics/undersea.jpg');
    g.load.image('phaser', assets + 'sprites/phaser.png');
    g.load.script('filterY', filters + 'CheckerWave.js');
  };
  gCreate = function(){
    var bg, logo;
    g.add.sprite(0, 0, 's');
    bg = g.add.sprite(0, 0);
    bg.width = 800;
    bg.height = 600;
    filter = g.add.filter('CheckerWave', 800, 600);
    filter.alpha = 0.2;
    bg.filters = [filter];
    logo = g.add.sprite(g.world.centerX, g.world.centerY, 'phaser');
    logo.anchor.setTo(0.5, 0.5);
  };
  gUpdate = function(){
    filter.update();
  };
  gRender = function(){};
  g = new Phaser.Game(800, 600, Phaser.WEBGL, '', {
    preload: gPreload,
    create: gCreate,
    update: gUpdate,
    render: gRender
  });
}).call(this);
