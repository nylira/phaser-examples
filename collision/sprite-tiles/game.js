// Generated by LiveScript 1.2.0
(function(){
  'use strict';
  var g, tiles, sprite, carryOn, collisionHandler;
  g = new Phaser.Game(800, 600, Phaser.CANVAS, '', {
    preload: preload,
    create: create,
    update: update,
    render: render
  });
  function preload(){
    g.load.spritesheet('tiles', '../../../phaser/examples/assets/tilemaps/tiles/platformer_tiles.png', 16, 16);
    return g.load.image('carrot', '../../../phaser/examples/assets/sprites/carrot.png');
  }
  function create(){
    var i$, i, tile;
    g.stage.backgroundColor = '#2d2d2d';
    tiles = g.add.group();
    for (i$ = 0; i$ < 40; ++i$) {
      i = i$;
      tile = tiles.create(100 + i * 16, 300, 'tiles', 4);
      tile.body.immovable = true;
    }
    sprite = g.add.sprite(300, 150, 'carrot');
    sprite.name = 'mushroom';
    sprite.body.collideWorldBounds = true;
    sprite.body.velocity.x = 40;
    sprite.body.velocity.y = 120;
    sprite.body.bounce.setTo(1, 1);
    return g.input.onDown.add(carryOn, this);
  }
  carryOn = function(){
    g.paused = false;
    return console.log('nopause!');
  };
  function update(){
    return g.physics.collide(sprite, tiles, collisionHandler, null, this);
  }
  collisionHandler = function(sprite, tile){
    tile.alpha = 0.5;
    console.log('---------------------------------------------');
    console.log(tile.body);
    return g.paused = true;
  };
  function render(){
    return g.debug.renderPhysicsBody(sprite.body);
  }
}).call(this);
